import { NgIfPermissionsDirective } from './ng-if-permissions.directive';

describe('NgIfPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
